<?xml version="1.0" encoding="utf-8"?>

<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/main_fragment_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="?android:actionBarSize"
        app:layoutManager="androidx.recyclerview.widget.StaggeredGridLayoutManager"
        tools:listitem="@layout/item_recycler_main" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/fragment_main_bottom_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        style="@style/AppBar"
        app:fabAlignmentMode="center"
        app:menu="@menu/appbar_menu"
        android:paddingEnd="@dimen/primary_padding"
        android:paddingStart="@dimen/primary_padding" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:transitionName="transit"
        android:id="@+id/fragment_main_floating_action_button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        style="@style/FloatingActionButton"
        android:contentDescription="@string/description_button_create"
        android:src="@drawable/ic_baseline_add_24"
        app:layout_anchor="@id/fragment_main_bottom_app_bar"/>


</androidx.coordinatorlayout.widget.CoordinatorLayout>